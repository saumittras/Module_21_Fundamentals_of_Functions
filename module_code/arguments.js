function doubleIt(number) {
  const doubled = number * 2;
  console.log(number, doubled);
}

doubleIt(45);

const number = 55;
doubleIt(number);
function difference(num1, num2) {
  const diff = num1 - num2;
  console.log(num1, num2, "dufference is", diff);
}

const fage = 40;
const myage = 10;
difference(fage, myage);
