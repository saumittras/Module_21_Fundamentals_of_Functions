function tenTimes(number) {
  const result = number * 10;
  return result;
}

function cuthalf(number) {
  const half = number / 2;
  return half;
}

tenTimes(5);
const output = tenTimes(12);
console.log(output);
