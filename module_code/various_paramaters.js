/**
 *
 * for a given string tell me weather it has even number of characters or not
 */

function evenSizeedString(str) {
  const size = str.length;
  console.log(str, size);
}
evenSizeedString("dhaka");
